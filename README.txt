En las clases que hemos tenido, vimos cómo crear un nuevo proyecto de FastAPI: creamos un entorno virtual, instalamos ahí el paquete de fastapi standard, escribíamos el app.py (o main.py) y ya lo podíamos levantar para que funcione. Ya con el proyecto funcional creamos en memoria unos datos simulando una tabla de una base de datos y escribimos 4 path operations para ver los datos, crear nuevos, modificarlos y borrarlos.

En esta tarea les pido que hagan todo este proceso y me entreguen un .zip con toda la carpeta del proyecto. Alternativamente, si se animan, me lo pueden compartir por github compartiéndome el link del repositorio.

¿Qué tiene que tener el práctico?: 
Un entorno virtual con fastapi[standard] (y/o un requirements.txt*)
un main o app.py con:
      Una variable que simule una tabla en la base de datos; con temática libre (excepto videojuegos, puede ser cualquier cosa).
      5 path operations; cada una con uno de los métodos http que vimos **. Estos deben modificar la variable que simula la db según su respectivo método

*REQUIREMENTS.TXT:
Si el proyecto incluye un requirements, no hace falta subir el venv (siempre y cuando el requirements se haya creado adentro del entorno virtual, sino queda cualquier cosa)

**5 Path Operations: Pido 2 GET (get todo y get por id), 1 post, 1 put y 1 delete. El patch no es requisito pero si lo quieren agregar son bienvenidos.

Debería haber quedado claro pero sino me escriben en el comentario de la tarea. Tienen hasta el viernes que viene. Éxitos!!